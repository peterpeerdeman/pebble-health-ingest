# Copy to .env and fill in. `docker compose` reads this file automatically.

# Shared secret between the watchapp and the ingest service. Generate one with:
#   openssl rand -hex 24
INGEST_TOKEN=

# Grafana admin password (first start only).
GRAFANA_ADMIN_PASSWORD=admin
